"""Terran builds.

Empty for now — near-term focus is Zerg. To add one, drop a module here that
defines a module-level `BUILD = BuildDefinition(..., race=Race.Terran)` and it
is picked up automatically; also add a matching opening to `terran_builds.yml`
(ares requires that exact filename) or `tests/test_builds.py` will fail.
"""
