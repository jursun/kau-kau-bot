"""Protoss builds.

Drop a module here that defines a module-level
`BUILD = BuildDefinition(..., race=Race.Protoss)` and it is picked up
automatically; also add a matching opening to `protoss_builds.yml`
(ares requires that exact filename) or `tests/test_builds.py` will fail.
"""
